<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="./vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="tests/bootstrap.php"
         colors="true"
         verbose="true"
         stopOnFailure="false">

    <testsuites>
        <testsuite name="Synkrop Unit Tests">
            <directory>tests</directory>
        </testsuite>
    </testsuites>

    <!-- #32: tests de integracion (grupo "integration") requieren red y una
         cuenta sandbox real de Bsale, excluidos del run normal. Se activan
         seteando BSALE_SANDBOX_TOKEN y pasando el grupo explicito a phpunit
         (ver README de tests/integration para el comando exacto). -->
    <groups>
        <exclude>
            <group>integration</group>
        </exclude>
    </groups>

    <coverage>
        <include>
            <directory suffix=".php">synkrop/classes</directory>
        </include>
    </coverage>
</phpunit>
